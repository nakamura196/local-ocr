# ビルド時に取り込む llama.cpp（scripts/fetch-binaries.zsh / .ps1 が置く）。
# 25MB(mac) / 90MB(win) あり、版を上げるたびに丸ごと入れ替わるので履歴に入れない。
binaries/

build/
dist/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# 公証の資格情報。1Password への op:// 参照だけを書くが、項目名も外に出さない。
.env
.env.*
!.env.example
